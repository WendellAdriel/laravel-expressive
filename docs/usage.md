# Usage

Document the primary Expressive workflow here.

```php
// Add a focused example for expressive once the package API is defined.
```

Keep examples short and copyable. Prefer showing how the package is used inside a normal Laravel application instead of documenting internals.

## Configuration

If Expressive publishes configuration, explain the important options and their defaults here.
